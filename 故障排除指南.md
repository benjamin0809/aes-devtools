# AES DevTools 故障排除指南

## 常见错误及解决方案

### 1. "Cannot read properties of undefined (reading 'addListener')" 错误

**错误原因**: Chrome DevTools API 还没有完全初始化

**解决方案**:
1. **重新加载扩展**: 在 `chrome://extensions/` 页面重新加载扩展
2. **等待初始化**: 扩展需要几秒钟时间来初始化Chrome API
3. **检查环境**: 确保在Chrome DevTools环境中运行

**预防措施**: 代码已经添加了API可用性检查和重试机制

---

### 2. CSS样式不显示

**错误原因**: 样式文件路径错误或文件不存在

**解决方案**:
1. **检查文件结构**:
   ```
   webbas-devtools/
   ├── styles/
   │   ├── themes.css
   │   └── main.css
   └── panel.html
   ```

2. **验证HTML链接**:
   ```html
   <link rel="stylesheet" href="styles/themes.css">
   <link rel="stylesheet" href="styles/main.css">
   ```

3. **检查文件权限**: 确保CSS文件可读

---

### 3. JavaScript模块未加载

**错误原因**: 模块文件路径错误或语法错误

**解决方案**:
1. **检查文件结构**:
   ```
   webbas-devtools/
   ├── js/
   │   ├── aes-module.js
   │   ├── utils.js
   │   ├── network.js
   │   ├── storage.js
   │   └── ui.js
   └── panel.html
   ```

2. **验证脚本引用**:
   ```html
   <script src="js/aes-module.js"></script>
   <script src="js/utils.js"></script>
   <script src="js/network.js"></script>
   <script src="js/storage.js"></script>
   <script src="js/ui.js"></script>
   <script src="panel.js"></script>
   ```

3. **检查控制台错误**: 查看是否有JavaScript语法错误

---

### 4. 扩展不显示在DevTools中

**错误原因**: 扩展未正确安装或manifest配置错误

**解决方案**:
1. **重新安装扩展**:
   - 打开 `chrome://extensions/`
   - 开启"开发者模式"
   - 删除旧版本
   - 重新加载扩展

2. **检查manifest.json**:
   ```json
   {
     "name": "AES DevTools",
     "version": "2.0.0",
     "devtools_page": "devtools.html",
     "permissions": ["storage"],
     "manifest_version": 2
   }
   ```

3. **重启浏览器**: 完全关闭Chrome后重新打开

---

### 5. 网络请求不显示

**错误原因**: 网络监听器未正确设置

**解决方案**:
1. **等待初始化**: 扩展需要时间初始化Chrome API
2. **检查权限**: 确保扩展有网络访问权限
3. **刷新页面**: 在扩展加载后刷新测试页面

---

### 6. 存储管理功能不工作

**错误原因**: Chrome DevTools API不可用

**解决方案**:
1. **等待API初始化**: 扩展会自动重试API连接
2. **使用本地模式**: 扩展会在API不可用时使用本地数据
3. **检查控制台**: 查看是否有相关警告信息

---

## 调试步骤

### 步骤1: 检查扩展状态
1. 打开 `simple_test.html` 页面
2. 点击"检查扩展状态"按钮
3. 查看各项功能的状态

### 步骤2: 检查控制台错误
1. 按 `F12` 打开DevTools
2. 查看Console标签页
3. 寻找红色错误信息

### 步骤3: 检查网络请求
1. 在DevTools中切换到Network标签页
2. 刷新页面
3. 查看是否有CSS和JS文件加载失败

### 步骤4: 检查元素
1. 在DevTools中切换到Elements标签页
2. 检查HTML结构是否正确
3. 查看CSS样式是否应用

---

## 环境要求

### 浏览器要求
- **Chrome**: 版本 88 或更高
- **Edge**: 版本 88 或更高（基于Chromium）

### 扩展权限
- `storage`: 存储访问权限
- `devtools`: DevTools集成权限

### 系统要求
- Windows 10/11, macOS 10.14+, Linux
- 至少 4GB RAM
- 支持现代JavaScript特性

---

## 测试方法

### 1. 基本功能测试
使用 `simple_test.html` 页面测试：
- 扩展状态检查
- 基本功能验证
- 存储功能测试
- 网络请求测试

### 2. 完整功能测试
使用 `test.html` 页面测试：
- 网络监控功能
- 存储管理功能
- AES解密功能

### 3. 扩展集成测试
1. 安装扩展
2. 打开任意网页
3. 按 `F12` 打开DevTools
4. 查找"AES DevTools"标签页

---

## 常见问题FAQ

### Q: 扩展安装后没有显示？
**A**: 确保在DevTools中查找，不是在普通页面中

### Q: 样式显示异常？
**A**: 检查CSS文件路径和文件完整性

### Q: 功能按钮不响应？
**A**: 等待扩展完全初始化，通常需要几秒钟

### Q: 网络请求不显示？
**A**: 确保在扩展加载后发送请求

### Q: 存储数据不保存？
**A**: 检查Chrome DevTools API是否可用

---

## 获取帮助

如果以上方法都无法解决问题：

1. **查看控制台日志**: 收集错误信息
2. **检查文件完整性**: 确保所有文件都存在
3. **重新安装扩展**: 完全删除后重新安装
4. **联系支持**: 提供详细的错误信息和环境描述

---

## 预防措施

1. **定期更新**: 保持Chrome浏览器最新版本
2. **权限管理**: 只给扩展必要的权限
3. **环境隔离**: 在测试环境中先验证功能
4. **备份配置**: 保存重要的扩展配置

---

**记住**: 大多数问题都可以通过重新加载扩展和等待初始化来解决！ 