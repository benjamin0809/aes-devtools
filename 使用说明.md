# AES DevTools 使用说明

## 概述
AES DevTools 是一个 Chrome 浏览器扩展，专门用于监控网络请求、管理浏览器存储，并自动检测和解密 AES 加密的数据。

## 安装和配置

### 1. 安装扩展
1. 打开 Chrome 浏览器
2. 进入扩展管理页面 (`chrome://extensions/`)
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `webbas-devtools` 文件夹

### 2. 使用扩展
1. 在任意网页上按 `F12` 或右键选择"检查"
2. 在 DevTools 中找到 "AES DevTools" 标签页
3. 点击进入即可使用

## 功能特性

### 网络监控
- **实时监控**: 自动捕获所有 HTTP/HTTPS 请求
- **智能过滤**: 按方法、状态码、URL 等条件过滤请求
- **自动解密**: 自动检测并解密 AES 加密的请求/响应
- **批量操作**: 支持批量解密、导出等操作
- **搜索历史**: 记录搜索历史，快速重复搜索

### 存储管理
- **双重存储**: 同时管理 localStorage 和 sessionStorage
- **实时编辑**: 双击存储项进行实时编辑
- **搜索功能**: 快速定位特定的存储项
- **导入导出**: 支持数据的备份和恢复
- **批量操作**: 支持批量删除、清空等操作

### 主题支持
- **双主题**: 支持浅色和深色两种主题
- **自动切换**: 点击主题按钮即可切换
- **记忆功能**: 自动记住用户的主题选择

## 使用方法

### 网络监控
1. 切换到"网络监控"标签页
2. 在搜索框中输入关键词过滤请求
3. 使用方法过滤器和状态过滤器进一步筛选
4. 点击请求项查看详细信息
5. 右键点击请求项使用上下文菜单

### 存储管理
1. 切换到"存储管理"标签页
2. 选择 localStorage 或 sessionStorage
3. 双击存储项进行编辑
4. 使用搜索功能快速定位项目
5. 使用工具栏按钮进行批量操作

### 搜索功能
1. 在搜索框中输入要查找的内容
2. 系统会自动记录搜索历史
3. 点击搜索历史按钮查看历史记录
4. 点击历史记录项快速重复搜索

### 上下文菜单
右键点击网络请求项可以：
- 复制 URL
- 复制请求内容
- 复制响应内容
- 复制解密结果
- 导出请求数据

## 快捷键
- `Ctrl+F`: 聚焦搜索框
- `Ctrl+D`: 批量解密
- `Ctrl+E`: 导出数据
- `Ctrl+L`: 清空列表

## 故障排除

### 样式不显示
如果样式文件没有正确加载：
1. 检查 `styles` 文件夹是否存在
2. 确认 `themes.css` 和 `main.css` 文件存在
3. 检查浏览器控制台是否有错误信息

### 功能不工作
如果某些功能不工作：
1. 检查浏览器控制台的错误信息
2. 确认所有 JavaScript 模块文件存在
3. 尝试刷新页面或重新加载扩展

### 网络请求不显示
如果网络请求没有显示：
1. 确认扩展已正确安装
2. 检查 DevTools 中是否有 AES DevTools 标签页
3. 确认网络请求是在扩展加载后发送的

## 开发说明

### 文件结构
```
webbas-devtools/
├── panel.html          # 主界面文件
├── panel.js            # 主入口文件
├── styles/             # 样式文件目录
│   ├── themes.css      # 主题样式
│   └── main.css        # 主要样式
├── js/                 # JavaScript 模块目录
│   ├── utils.js        # 工具函数
│   ├── aes.js          # AES 解密功能
│   ├── network.js      # 网络监控功能
│   ├── storage.js      # 存储管理功能
│   └── ui.js           # 用户界面功能
└── manifest.json       # 扩展配置文件
```

### 模块化设计
扩展采用模块化设计，每个功能模块独立：
- **utils.js**: 提供通用工具函数
- **aes.js**: 处理 AES 加密/解密
- **network.js**: 管理网络请求监控
- **storage.js**: 管理浏览器存储
- **ui.js**: 处理用户界面交互

### 自定义开发
如需添加新功能：
1. 在相应的模块文件中添加功能
2. 在 `panel.js` 中初始化新功能
3. 在 `panel.html` 中添加相应的 HTML 元素
4. 在 CSS 文件中添加样式

## 更新日志

### v2.0.0 (模块化重构)
- 重构为模块化架构
- 分离 HTML、CSS 和 JavaScript
- 优化代码结构和可维护性
- 添加搜索历史和上下文菜单
- 改进主题切换功能

### v1.0.0 (初始版本)
- 基础网络监控功能
- AES 自动解密
- 存储管理功能
- 基础主题支持

## 技术支持
如果遇到问题或需要帮助，请：
1. 检查浏览器控制台的错误信息
2. 查看扩展的版本和配置
3. 确认浏览器版本兼容性
4. 尝试重新安装扩展

## 许可证
本扩展仅供学习和开发使用，请遵守相关法律法规。 